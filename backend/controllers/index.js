const auxilier = require("./auxilier");
const operation = require("./operation");
const arret = require("./arret");
const liaison = require("./liaison");
const groupe = require("./groupe");

module.exports = {
  auxilier,
  operation,
  arret,
  liaison,
  groupe,
};
